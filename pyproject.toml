[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pii-proxi"
version = "0.1.0"
description = "Local privacy-filter proxy for coding assistants (Claude Code, Codex CLI, etc)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "pii-proxi" }]
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "httpx>=0.27",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "typer>=0.12",
    "tiktoken>=0.7",
    "numpy>=1.26",
    "huggingface_hub>=0.22",
]

[project.optional-dependencies]
mlx = [
    "mlx>=0.18",
    # The ``openai_privacy_filter`` architecture isn't in the 0.1.0 PyPI
    # release yet; install from git (same maintainer who publishes the
    # mlx-community model conversions). Switch back to a PyPI pin once a
    # release ships with ``mlx_embeddings/models/openai_privacy_filter.py``.
    "mlx-embeddings @ git+https://github.com/Blaizzy/mlx-embeddings.git",
]
onnx = ["onnxruntime>=1.17"]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "respx>=0.21",
    "ruff>=0.4",
]

[project.scripts]
pii-proxi = "pii_proxi.cli:app"

[tool.hatch.metadata]
# The ``mlx`` extra pulls ``mlx-embeddings`` from a git URL (see comment in
# ``[project.optional-dependencies].mlx``); hatch refuses direct references
# without this opt-in.
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/pii_proxi"]

[tool.hatch.build.targets.sdist]
include = ["/src", "/README.md", "/pyproject.toml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["src"]
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.ruff]
line-length = 100
target-version = "py311"
